package com.bot.logic.states;

import com.bot.logic.BotContext;

public class PatrolState implements BotState {
    @Override
    public void execute(BotContext ctx) {
        // Estado de varredura visual provido pelo EntityManager global
    }
}